server:
  port: 8888

spring:
  profiles:
    active: native # le digo al config server que carge los archivos de forma local -> classpath en este caso
  application:
    name: msvc-config

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configuration